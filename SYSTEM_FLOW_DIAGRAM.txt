# System Flow Diagram

## Optimized Ordering System - Like Jollibee/McDonald's

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CUSTOMER JOURNEY                             │
└─────────────────────────────────────────────────────────────────────┘

STEP 1: CUSTOMER ORDERS
┌──────────────────────┐
│   Customer visits    │
│  customer_order.php  │
│                      │
│  - Browse menu       │
│  - Add to cart       │
│  - Enter name/table  │
│  - Place order       │
└──────────┬───────────┘
           │
           ▼
    ┌─────────────┐
    │   DATABASE  │
    │   orders    │
    │             │
    │ customer_id │ ← Customer's user ID (or NULL for walk-in)
    │ processed_by│ ← NULL (no cashier yet)
    │ status      │ ← "pending"
    │ payment     │ ← "unpaid"
    └──────┬──────┘
           │
           ▼

STEP 2: KITCHEN PREPARES
┌──────────────────────┐
│  Barista/Kitchen     │
│   barista.php        │
│                      │
│  - See new orders    │
│  - Update status:    │
│    pending →         │
│    preparing →       │
│    ready             │
└──────────┬───────────┘
           │
           ▼
    ┌─────────────┐
    │   DATABASE  │
    │   orders    │
    │             │
    │ status      │ ← "ready"
    │ payment     │ ← Still "unpaid"
    └──────┬──────┘
           │
           ▼

STEP 3: CASHIER PROCESSES PAYMENT
┌──────────────────────┐
│      Cashier         │
│    orders.php        │
│                      │
│  - Customer comes    │
│  - Click "Process    │
│    Payment" button   │
└──────────┬───────────┘
           │
           ▼
    ┌─────────────┐
    │   DATABASE  │
    │   orders    │
    │             │
    │ processed_by│ ← Cashier's user ID
    │ status      │ ← "completed"
    │ payment     │ ← "paid"
    └─────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    ALTERNATIVE: WALK-IN ORDER                        │
└─────────────────────────────────────────────────────────────────────┘

CASHIER TAKES ORDER FOR WALK-IN
┌──────────────────────┐
│      Cashier         │
│   neworder.php       │
│                      │
│  - Take order        │
│  - Input items       │
│  - Process payment   │
│    immediately       │
└──────────┬───────────┘
           │
           ▼
    ┌─────────────┐
    │   DATABASE  │
    │   orders    │
    │             │
    │ customer_id │ ← NULL (walk-in)
    │ processed_by│ ← Cashier's user ID
    │ status      │ ← "completed"
    │ payment     │ ← "paid"
    └─────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                         USER ROLES                                   │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   CUSTOMER   │  → Can place orders via customer_order.php
│              │  → Orders linked to their account
└──────────────┘

┌──────────────┐
│   CASHIER    │  → Takes walk-in orders (neworder.php)
│              │  → Processes payments (orders.php)
│              │  → Their ID saved in "processed_by"
└──────────────┘

┌──────────────┐
│   BARISTA    │  → Updates order preparation status
│              │  → No payment access
└──────────────┘

┌──────────────┐
│    ADMIN     │  → Full access to everything
│              │  → Can do all roles
└──────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                      DATABASE STRUCTURE                              │
└─────────────────────────────────────────────────────────────────────┘

orders table:
┌─────────────────┬──────────┬────────────────────────────────────┐
│ Column          │ Type     │ Description                        │
├─────────────────┼──────────┼────────────────────────────────────┤
│ id              │ INT      │ Primary key                        │
│ customer_id     │ INT NULL │ Who ordered (NULL = walk-in)       │
│ processed_by    │ INT NULL │ Which cashier processed payment    │
│ customer_name   │ VARCHAR  │ Display name                       │
│ table_num       │ VARCHAR  │ Table number                       │
│ total           │ DECIMAL  │ Order total                        │
│ status          │ ENUM     │ pending/preparing/ready/completed  │
│ payment_status  │ ENUM     │ unpaid/paid                        │
│ created_at      │ TIMESTAMP│ When order was placed              │
│ updated_at      │ TIMESTAMP│ Last update                        │
└─────────────────┴──────────┴────────────────────────────────────┘

order_items table:
┌─────────────────┬──────────┬────────────────────────────────────┐
│ Column          │ Type     │ Description                        │
├─────────────────┼──────────┼────────────────────────────────────┤
│ id              │ INT      │ Primary key                        │
│ order_id        │ INT      │ Foreign key to orders              │
│ product_name    │ VARCHAR  │ Product name                       │
│ price           │ DECIMAL  │ Price per item                     │
│ quantity        │ INT      │ Quantity ordered                   │
└─────────────────┴──────────┴────────────────────────────────────┘
```

## Key Benefits

✅ **Faster Service** - Customers order themselves
✅ **Less Cashier Work** - Only handle payments
✅ **Better Tracking** - Know who ordered and who processed
✅ **Scalable** - Can add kiosks, mobile apps later
✅ **Modern** - Like major fast-food chains
✅ **Flexible** - Still supports walk-in orders
